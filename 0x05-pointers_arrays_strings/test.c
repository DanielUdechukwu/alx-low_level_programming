#include <stdio.h>

int main(void)
{
	int size = 10;
	int aba[size] = {1, 2, 3, 4, 5};

	for (int i = 0; i <= size; i++)
	{
		int count = 0;
		count++;
	}

	printf(count);
	printf("\n");
return (0);
}
