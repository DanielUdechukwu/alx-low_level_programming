#include "main.h"

/**
 * _strlen - Function to check length of string
 * @s: Parameter for checking array index
 * Return: Always 0
 */


int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{

	}
return (i);
}
